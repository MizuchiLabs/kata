version: "3"

tasks:
  lint:
    desc: Run go vet & lint
    ignore_error: true
    cmds:
      - go mod tidy
      - go fmt ./...
      - go vet ./...
      - govulncheck ./...
      - go test -race -count=1 -cover ./...
      - golangci-lint run --fix
